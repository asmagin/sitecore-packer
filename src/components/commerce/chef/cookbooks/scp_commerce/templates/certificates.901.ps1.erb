$prefix = "<%= @sitecore['prefix'] %>"

$certParams =
@{
    Path            = "$PSScriptRoot\createcert.json"
    CertificateName = "$prefix.commerce"
}
Install-SitecoreConfiguration @certParams -Verbose